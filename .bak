- id=6
- pageName = 'Q'+id
include ../mixins/header
+header(pageName)
  link(rel="stylesheet", src="/assets/css/editor.css")
body
  include ../mixins/navbar
  +navbar(pageName)
  section.intro(style="min-height:100vh;min-width:100vw;")
    .container
      .row.text-center
        .col-sm-12
          .heading-count
          h2 Challenge ##{id}:
      .row.text-center
        .col-sm-12
          p.col-sm-12
            | Write a JavaScript function called telegraph()
            | that converts #[strong any] word into Morse code. The function will convert a string of characters into .'s, -'s, and spaces.
      .row
        .col-sm-6
            pre.
              function telegraph(word) {

            textarea(name="answer")
              // change this code
              var result = ""

              // convert word into Morse code and store it in result

              // return result
              return result
            pre.
              }

              // should return the string ".... . .-.. .-.. ---"
              telegraph("Hello");

              // should return the string ".-- --- .-. .-.. -.."
              telegraph("World");
        .col-sm-6
      if(img)
        .row
          .col-sm-12.text-center
            img.img-fluid.img-challenge(src=`/assets/img/challenges/${id}.png`)
      .row.text-center
        .col-sm-12
          form(method="POST")
            button.btn.form-group Submit

    #editor
  script(src="/assets/js/menu.js", type="javascript")
  script(src="/assets/js/ace-builds/src/ace.js", type="javascript", charset="utf-8")
  script
    include ../../assets/js/e.js
